# Part 2 — LLM-evaluation check. Kept separate so the Playwright suite has zero Python
# deps; install this only to run the eval.
deepeval>=4.2,<5
pytest>=8
pytest-rerunfailures>=14   # retry a transient judge-API hiccup (pytest.ini)
requests>=2.31

# Judge SDKs. google-genai is the default (free Gemini tier); the others are here so
# DEEPEVAL_JUDGE=anthropic|openai|ollama work without a second install.
google-genai>=1.0
anthropic>=0.40
openai>=1.40
ollama>=0.3
